/*var link = window.location.pathname;
$(function(){
	$('.portfolio').hover(
      function () {
        $(this).addClass('active');
      },
      function () {
        $('.nav li').each(function(index, elem) {
			if ( $(elem).hasClass('active') && $(elem) != $(this) )  {
				$('.portfolio').removeClass('active');
				console.log(link);
				return false;
			} 
		});
      }
    );
});
*/